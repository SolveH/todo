pipeline {
    agent { 
        dockerfile { 
            dir 'web'
        } 
    }
    stages {
        stage('build') {
            steps {
                echo 'things should be fine by now'
            }
        }
    }
}